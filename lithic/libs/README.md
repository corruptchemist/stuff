Drop `ToughAsNails-neoforge-1.21.1-*.jar` and `GlitchCore-neoforge-1.21.1-*.jar`
here and set `use_maven_tan=false` in `gradle.properties` if the Maven
coordinates do not resolve for you. Jars in this directory are ignored by git.
